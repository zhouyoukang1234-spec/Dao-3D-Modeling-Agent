"""DAO addon — non-GUI init hook.

FreeCAD imports this in both console and GUI modes. Nothing is needed here for
the AI panel (that is GUI-only, see InitGui.py); kept so the addon is a complete,
well-formed FreeCAD module.
"""
